10 PRINT "USING NO MORE THAN 25 CHARACTERS, ENTER"
20 LINE INPUT "LAST NAME, COMMA, FIRST NAME:"; NAME$
30 IF LEN(NAME$) < 1 OR LEN(NAME$) > 25 THEN 10
40 IF LEN(NAME$) >= 1 AND LEN(NAME$) <= 50 THEN PRINT NAME$

